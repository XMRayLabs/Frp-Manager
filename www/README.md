# frp-manager Web

This directory contains the Next.js web console for frp-manager.

## Development

```bash
pnpm install
pnpm run dev
```

## Checks

```bash
pnpm run lint
pnpm run build
```

The backend API is expected to be available through the configured panel endpoint during local development.

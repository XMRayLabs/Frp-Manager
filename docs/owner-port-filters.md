# 所属用户和端口筛选

客户端列表与隧道管理增加“所属用户”下拉框。选项来自当前账号有权查看的资源，按数据库 UserID 识别归属，不依赖客户端或代理名称。普通用户的筛选不会扩大访问权限；服务端列表不增加此功能。

客户端所属用户筛选覆盖全部分页；隧道保留原有所属客户端、所属服务端、协议及状态筛选，并可组合使用。

端口范围输入改为“搜索端口”，按字符串包含关系同时匹配本地端口和服务器端口。例如 6000 匹配 6000、16000、60001，但不匹配 6001。空输入不限制端口。切换筛选回到第一页，重置清除所属用户和端口条件。

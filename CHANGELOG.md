# 更新日志

本文件记录每个正式版本面向用户的主要变化。发布日期使用项目发布者所在时区的日期。

## 1.0.6 - 2026-09-09

- 修复 macOS 系统服务缺少 HOME 时无法定位自动接入身份目录的问题；保留已有身份路径。
- 非调试模式也记录初始化和启动失败原因，避免服务反复退出却没有错误日志。

- 修复 macOS 客户端重复启动或重新安装服务时的 launchctl load 错误；使用明确的系统服务目标并等待启动、卸载状态完成。
- macOS 保存并连接不再忽略停止、卸载错误；服务运行状态检查通过后才显示启动成功。
- 清理客户端错误日志中的 ANSI 颜色控制符。

## 1.0.5 - 2026-09-08

- 客户端和服务端改为使用专属设备密钥识别身份，名称可修改；改名后旧启动命令中的 `-i` 不会覆盖面板的新名称。
- 自动接入默认生成随机名称，保留所属用户前缀；随机名称与设备密钥分开，避免泄露凭据。
- 修改名称立即释放旧名称，不保留历史别名；已有连接、隧道和权限关联自动迁移。
- 删除节点时撤销旧密钥并断开管理连接，同时清理其隧道和相关子配置，避免新设备复用名称后继承已删除设备的关联。
- 修复卸载、删除后重新接入时复用失效缓存身份的问题；新版客户端“保存并连接”会检查缓存，仅明确重新接入时重新注册，普通重启不会自动恢复已删除设备。
- 设备认证失败显示具体原因；日志订阅与远程指令绑定认证连接，防止名称复用造成串流或误操作。
- 正式版本发布时从更新日志提取对应版本说明，附在 Release 和产物中。

## 1.0.4 - 2026-09-08

- 自动接入后的客户端和服务端支持修改实际 ID；关联配置、隧道和在线连接会自动迁移，设备无需重新安装或重新接入。
- 概览中的“待处理客户端”和“待处理服务端”可直接跳转到已经筛选好的对应列表。
- 客户端配置页增加独立的快捷添加区：HTTP、SOCKS5 和 TCP 只显示常用必填项，自动使用当前客户端并生成隧道名称。
- 增加“其他设置”入口，继续保留完整协议和高级隧道配置。

## 1.0.3 - 2026-09-08

- 客户端与隧道列表增加所属用户筛选，便于管理员按 admin、ig01 等账户查看资源。
- 隧道端口筛选改为直接搜索端口，可查找包含指定数字的本机端口和服务器端口。
- 手动创建客户端和服务端改为点击后直接打开创建窗口。
- 移除侧边栏“工作空间”、页面重复标题和操作菜单中重复的启动命令，减少界面占用。
- 重构客户端和服务端配置页面，分离常用连接配置、隧道配置和完整 JSON 编辑，并保留高级配置字段。

## 1.0.2 - 2026-09-08

- 增加 HTTP 代理、SOCKS5 代理和 TCP 端口映射的快捷创建流程；常见场景可使用本机端口与服务器端口相同的配置。
- 隧道名称改为自动生成，格式为“用户.客户端名.服务端名.服务器端口.协议”。
- 概览改为显示客户端和服务端的“在线数量/总数量”，并增加待处理客户端、待处理服务端统计。
- 待处理范围涵盖未完成配置、配置错误、在线运行异常和需要升级；正常离线不再计入待处理。
- 自动接入令牌改为持久化固定令牌，允许在二次确认后修改。
- 旧版版本号为 `main` 的客户端会提示需要升级。
- 用户管理支持按编号范围批量创建用户；初始密码可使用邮箱，并在首次登录时强制修改。

## 1.0.1 - 2026-09-08

- 建立重构后的 frp-manager 正式基线，提供管理面板、客户端和服务端管理、隧道配置、用户及权限管理。
- 客户端和服务端支持使用管理面板连接信息与接入令牌自动注册，并保存分配的设备身份。
- 内核启动时支持自动检查更新；更新成功后替换程序并重新启动。
- 建立 GitHub Actions 发布流程，生成核心程序、Windows、macOS、Android 客户端产物以及 Docker 镜像。
- 发布产物统一使用固定文件名，移除同一平台带版本号的重复文件。

## 1.0.0 - 2026-07-05

- 完成 frp-manager 首次完整重构，统一项目名称、程序命令、配置、文档、安装脚本和发布产物。
- 新增独立客户端应用，支持 Windows、macOS 和 Android；可在图形界面中完成接入、安装系统服务、启动、停止、查看状态与日志。
- 重构客户端和服务端自动接入：只需管理面板地址与接入令牌即可注册，面板自动分配并持久保存设备 ID 和密钥，默认作为长期节点使用。
- 新增启动自动更新机制：客户端和服务端启动时检查正式版本，校验下载产物后安全替换并重启；补充 Windows 独立替换进程及失败恢复处理。
- 重构账户、租户和权限体系，增加邀请注册、用户组、按用户及资源授权，并加强不同账户之间的数据隔离。
- 重构 Web 管理界面和状态请求，统一客户端、服务端、隧道、流量、远程终端、Worker 与 WireGuard 管理，并通过批量状态请求降低页面轮询开销。
- 加强默认安全配置：默认校验 TLS、限制可信反向代理、保护 Cookie、隐藏日志中的密钥与数据库连接信息，并增加安全响应头、请求大小限制和接口限流。
- 重构 Docker 与反向代理部署配置，提供只读容器、最小权限、持久化数据目录及 HTTPS/WSS 示例。
- 重建多平台发布流程，统一生成核心程序、客户端安装包、校验文件和标准版/workerd Docker 镜像。
